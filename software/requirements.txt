numpy
scipy
pandas
scikit-learn
joblib
matplotlib
paho-mqtt>=2.0
brainflow
# Raspberry Pi only (motor + encoder)
RPi.GPIO; platform_machine == "aarch64" or platform_machine == "armv7l"
smbus2
# Optional: live EMG viewer (leg.plots.stream_plot)
pyqtgraph==0.12.4
PyQt5; platform_machine != "aarch64"
